const order=["makeup","hårstyling","bridal","udkørsel"];document.addEventListener("DOMContentLoaded",(async()=>{const prisliste=document.getElementById("prisliste"),pakker=await fetch("pakker.json").then((response=>response.json())),categories={};pakker.forEach((pakke=>{const tag=pakke.tag;categories[tag]||(categories[tag]=[]),categories[tag].push(pakke)}));const orderedCategories=[];order.forEach((tag=>{categories[tag]&&(orderedCategories.push(categories[tag]),delete categories[tag])})),Object.keys(categories).forEach((tag=>{orderedCategories.push(categories[tag])})),orderedCategories.forEach(((category,index)=>{const categoryDiv=document.createElement("div");categoryDiv.classList.add("category");const h3=document.createElement("h3");h3.textContent=order[index]||"Andet",categoryDiv.appendChild(h3),category.map((pakke=>{const itemDiv=document.createElement("div");itemDiv.classList.add("item"),itemDiv.innerHTML=`\n                <div class="beskrivelse">\n                    <p>${pakke.label}</p>\n                    <p>tid: ${pakke.duration} min</p>\n                </div>\n                <div class="streg"></div>\n                <div class="pris">\n                    <p>${pakke.price} Kr.</p>\n                </div>\n            `,categoryDiv.appendChild(itemDiv)})),prisliste.appendChild(categoryDiv)}))}));